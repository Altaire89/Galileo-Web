[project]
name = "backend"
version = "0.1.0"
description = "REST API for the consultancy support platform"
requires-python = ">=3.12"
dependencies = [
	"flask>=3.1.0",
	"flask-cors>=5.0.0",
	"gunicorn>=23.0.0; platform_system != 'Windows'",
]
